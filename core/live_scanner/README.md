# Live Scanner

Live Scanner — built in Module 18. Orchestrates Modules 06-16 on a schedule
against current market data. Incremental — never re-scans full history
(except on a deliberate model-version change). Pure orchestration; re-
implements no domain logic that lives elsewhere.
