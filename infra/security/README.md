# Security Infra

Security Hardening — built in Module 24. Rate limiting, WAF/CDN guidance,
secrets audit, the global `platform_state` emergency flag
(`NORMAL`/`READ_ONLY`/`EMERGENCY`) checked by middleware in front of all
mutating endpoints, and audit log completeness review.
