# Config

Configuration — built in Module 02. Env-based config loading (dev/staging/
prod), a secrets-manager integration point, and typed config objects that
other modules import. Cross-cutting: no module-specific settings live here,
only the shared loading/typing mechanism.
